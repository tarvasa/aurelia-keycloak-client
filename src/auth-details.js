/**
 *  komponentti 
	<auth-details if.bind="keycloakViewModel.authenticated"> </auth-details>
	Tuo komponentti aktivoituu kun 
  view-modellin(keycloakViewModel? auth-view-view-model?)
  authenticated muuttujaan ilmestyy data 
  (=authentikointi on tehty ja se on onnistunut).

*/
